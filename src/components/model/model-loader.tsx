"use client";
import type {KiteModel} from "@/types/model";
export function ModelLoader({model,progress,status,webgpuReady,loading,ready,onLoad}:{model:KiteModel;progress:number;status:string;webgpuReady:boolean|null;loading:boolean;ready:boolean;onLoad:()=>void}){return <section className="loader-card"><div className="eyebrow">KITE CORE</div><h1>Local intelligence, everywhere.</h1><p className="muted">Inicialize um modelo quando quiser. O runtime do Kite executa localmente no navegador.</p><div className="model-meta"><div><span className="meta-label">Modelo</span><strong>{model.name}</strong></div><div><span className="meta-label">VRAM estimada</span><strong>{Math.round(model.vramRequiredMB)} MB</strong></div></div><div className="status-row"><span>WebGPU</span><strong>{webgpuReady===null?"verificando…":webgpuReady?"disponível":"indisponível"}</strong></div>{(loading||ready)&&<div className="progress-group"><div className="progress-label"><span>{ready?"Modelo pronto":status}</span><span>{progress}%</span></div><div className="progress-track"><div className="progress-value" style={{width:`${progress}%`}}/></div></div>}{!ready&&<button className="primary-button" disabled={loading||webgpuReady!==true} onClick={onLoad}>{loading?"Inicializando…":"Carregar modelo Starter"}</button>}{ready&&<div className="ready-banner">✓ Engine inicializado. O chat está pronto.</div>}</section>;}
